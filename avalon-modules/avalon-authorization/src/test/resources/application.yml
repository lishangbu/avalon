spring:
  main:
    banner-mode: off
  liquibase:
    drop-first: true
    change-log: classpath:db/changelog/db.changelog-test.yaml
mybatis-plus:
  mapper-locations: classpath:/mapper/**.xml
  global-config:
    banner: false
    db-config:
      id-type: assign_id
      table-format: "\"%s\""