<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.lishangbu.avalon.authorization.mapper.UserRoleRelationMapper">
  <resultMap id="userRoleRelationResultMap" type="io.github.lishangbu.avalon.authorization.entity.UserRoleRelation">
    <!--@Table user_role_relation-->
    <result column="user_id" property="userId"/>
    <result column="role_id" property="roleId"/>
  </resultMap>

  <sql id="userRoleRelationColumnList">
    user_id,
    role_id
  </sql>

  <!--新增用户角色关系-->
  <insert id="insert" keyProperty="" useGeneratedKeys="true">
    insert into user_role_relation
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="userId != null">
        user_id,
      </if>
      <if test="roleId != null">
        role_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="userId != null">
        #{userId},
      </if>
      <if test="roleId != null">
        #{roleId},
      </if>
    </trim>
  </insert>

  <delete id="deleteByUserId">
    delete from user_role_relation where user_id = #{userId}
  </delete>
</mapper>

