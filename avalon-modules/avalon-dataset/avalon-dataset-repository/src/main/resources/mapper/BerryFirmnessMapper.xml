<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.lishangbu.avalon.dataset.mapper.BerryFirmnessMapper">
  <!-- 通用查询映射结果 -->
  <resultMap id="BaseResultMap" type="io.github.lishangbu.avalon.dataset.entity.BerryFirmness">
    <id column="id" property="id"/>
    <result column="internal_name" property="internalName"/>
    <result column="name" property="name"/>
  </resultMap>

  <select id="selectList" resultMap="BaseResultMap" parameterType="map">
    SELECT id, internal_name, name
    FROM berry_firmness
    <where>
      <if test="berryFirmness.id != null">
        AND id = #{berryFirmness.id}
      </if>
      <if test="berryFirmness.internalName != null and berryFirmness.internalName != ''">
        AND internal_name LIKE CONCAT('%', #{berryFirmness.internalName}, '%')
      </if>
      <if test="berryFirmness.name != null and berryFirmness.name != ''">
        AND name LIKE CONCAT('%', #{berryFirmness.name}, '%')
      </if>
    </where>
    ORDER BY id ASC
  </select>
</mapper>
