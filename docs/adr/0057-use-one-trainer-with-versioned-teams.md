# Use one Trainer with multiple versioned Teams

每个账户首发只拥有一个可按冷却规则改名的 Trainer，每个 Trainer 最多保存二十支命名 Team。Challenge 与 Practice Battle 显式选择 Team 并冻结 Snapshot，分享导入创建独立副本；这取代多 Trainer 与唯一 Team 模型，使身份、战绩和并发约束稳定，同时支持多套竞技配队。
