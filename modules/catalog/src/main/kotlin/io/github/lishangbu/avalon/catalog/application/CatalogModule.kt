package io.github.lishangbu.avalon.catalog.application

/**
 * Catalog 是 Avalon 的共享定义型参考知识库。
 *
 * 当前阶段它负责维护：
 * - 属性、技能、道具、物种等定义事实
 * - 被多个上下文共享读取的规则事实
 * - 读多写少、由内容维护流程驱动的数据
 *
 * 它不负责：
 * - 玩家实例态数据
 * - Battle 会话或运行时 rule model
 * - 为了省事兜底所有“基础数据”的杂项存储
 *
 * 第一阶段优先落后台手工维护 CRUD，先稳定 schema、边界和读取契约。
 * 当前第一波已落属性定义、属性克制关系、性格定义、道具定义、技能定义、特性定义、
 * 成长率定义、物种定义、物种进化定义与物种特性关联，后续再向可学习技能等参考数据扩展。
 */
object CatalogModule