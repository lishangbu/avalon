# Domain docs

本仓库采用 single-context 领域文档布局。

工程技能探索或修改代码前应读取：

- 根目录 `CONTEXT.md`：稳定领域语言与应避免的同义词；
- `docs/adr/`：与当前修改相关的既有架构决策；
- `AGENTS.md`：仓库边界、技术栈路由与完成条件。

测试名称、接口、issue 和重构提案必须使用 `CONTEXT.md` 中的词汇。若方案与现有 ADR 冲突，应显式指出并确认是否取代原决定，不得静默覆盖。
