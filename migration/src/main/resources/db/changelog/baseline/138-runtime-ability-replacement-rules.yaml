databaseChangeLog:
- changeSet:
    id: 227-runtime-ability-replacement-rules
    author: codex
    comment: 为复制特性、木乃伊、甩不掉的气味与游魂补充运行时特性替换规则。
    changes:
    - sql:
        splitStatements: false
        stripComments: false
        sql: |
          insert into battle_ability_rule (
            id, ability_id, trigger_timing, effect_policy,
            trigger_order, description, enabled, sort_order
          )
          select coalesce((select max(id) from battle_ability_rule), 0) + source.row_no,
            ability.id, source.trigger_timing, source.effect_policy,
            100, source.description, true, 7200 + source.row_no
          from (
            values
              (1, 'trace', 'SWITCH_IN', 'switch-in-copy-opponent-ability', '出场时复制一名当前上场对手的运行时特性。'),
              (2, 'mummy', 'AFTER_CONTACT', 'contact-replace-attacker-ability-with-holder', '受到接触伤害后，把攻击者特性替换为自身当前特性。'),
              (3, 'lingering-aroma', 'AFTER_CONTACT', 'contact-replace-attacker-ability-with-holder', '受到接触伤害后，把攻击者特性替换为自身当前特性。'),
              (4, 'wandering-spirit', 'AFTER_CONTACT', 'contact-swap-abilities', '受到接触伤害后，与攻击者交换当前特性。')
          ) as source(row_no, ability_code, trigger_timing, effect_policy, description)
          join game_ability ability on ability.code = source.ability_code
          on conflict (ability_id, trigger_timing, effect_policy) do update set
            trigger_order = excluded.trigger_order,
            description = excluded.description,
            enabled = excluded.enabled,
            sort_order = excluded.sort_order;
