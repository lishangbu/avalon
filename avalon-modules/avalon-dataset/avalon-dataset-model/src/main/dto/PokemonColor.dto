export io.github.lishangbu.avalon.dataset.entity.PokemonColor

PokemonColorView {
    id
    internalName
    name
}

input SavePokemonColorInput {
    internalName
    name
}

input UpdatePokemonColorInput {
    id!
    internalName
    name
}

specification PokemonColorSpecification {
    id
    like/i(internalName)
    like/i(name)
}
